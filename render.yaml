services:
  - type: web
    name: avelora-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install --omit=dev
    startCommand: node dist/main.js
    healthCheckPath: /health
    autoDeploy: true
