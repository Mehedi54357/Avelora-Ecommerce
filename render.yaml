services:
  - type: web
    name: avelora-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: node dist/main.js
    healthCheckPath: /health
    autoDeploy: true
